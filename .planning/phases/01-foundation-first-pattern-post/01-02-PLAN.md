---
phase: 01-foundation-first-pattern-post
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - velite.config.ts
  - content/patterns/toast-notification-system.mdx
  - app/patterns/[slug]/page.tsx
  - lib/mdx-components.tsx
  - components/mdx/Callout.tsx
autonomous: true
requirements:
  - FOUND-02
  - PATT-01
  - PATT-03
  - SITE-01
  - SITE-03

must_haves:
  truths:
    - "Code blocks in the post render with accurate Shiki syntax highlighting for TS/TSX/JS/HTML/CSS"
    - "The Toast Pattern post follows the full 8-section template with genuine production-grade prose and static code snippets only (no live demo)"
    - "Each post route emits correct SEO metadata: Open Graph tags, a JSON-LD Article schema authored by 'Alejandro Arevalo', and title/description"
    - "The rendered post displays an estimated reading time"
    - "Headings have anchor ids and self-links for deep linking"
  artifacts:
    - velite.config.ts
    - content/patterns/toast-notification-system.mdx
    - app/patterns/[slug]/page.tsx
    - lib/mdx-components.tsx
  key_links:
    - "velite.config.ts MDX options run the plugin chain in order: remark-gfm -> rehype-slug -> rehype-autolink-headings -> rehype-pretty-code(+Shiki)"
    - "app/patterns/[slug]/page.tsx generateMetadata reads getPatternBySlug() -> emits OG tags + title/description; the page injects a JSON-LD <script> built from the same post"
    - "reading-time computed in velite transform -> exposed as post.readingTime -> rendered in the post header"
---

## Phase Goal

**As a** visiting engineer or hiring manager, **I want to** read one fully realized, correctly themed, syntax-highlighted, SEO-correct Pattern post, **so that** I can learn a production-grade mental model and judge how the author thinks.

## This Plan's Slice (Rich, discoverable post content)

Building on the Walking Skeleton from Plan 01, this slice turns the minimal seeded post into the real, complete Toast Pattern post: accurate syntax highlighting on every code block, the full 8-section template body, a visible reading time, anchor-linkable headings, and per-post SEO metadata (Open Graph + JSON-LD). After this plan, the post is genuinely readable and shareable. RSS/sitemap/robots are delivered in parallel by Plan 03.

<objective>
Layer the full remark/rehype + Shiki syntax-highlighting pipeline into the Velite MDX compile, author the complete 8-section Toast Pattern post, render the reading-time estimate, and emit per-post SEO metadata (Open Graph, JSON-LD Article schema, title/description).

Purpose: Deliver the actual reader-facing value of the phase — a production-grade, syntax-highlighted, SEO-correct Pattern post — on top of the proven pipeline.
Output: A complete, highlighted, SEO-correct Toast Pattern post at its route.
</objective>

<execution_context>
@C:/Users/Alex/workspace/blog/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Alex/workspace/blog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md
@.planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md
@.planning/phases/01-foundation-first-pattern-post/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files created or extended by THIS plan (new — exclude from drift/source-grounding checks):

- New file: `components/mdx/Callout.tsx` (presentational MDX component)
- Extended files (created by Plan 01, modified here): `velite.config.ts` (adds the full remark/rehype plugin chain to MDX options), `content/patterns/toast-notification-system.mdx` (full 8-section body), `app/patterns/[slug]/page.tsx` (adds `generateMetadata` + JSON-LD injection + reading-time render), `lib/mdx-components.tsx` (registers `Callout` and code-block presentation)
- Function: `generateMetadata({ params })` exported from `app/patterns/[slug]/page.tsx`
- JSON-LD Article object with `author: { "@type": "Person", name: "Alejandro Arevalo" }` (D-09)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the full remark/rehype + Shiki plugin chain into the Velite MDX compile</name>
  <files>velite.config.ts, lib/mdx-components.tsx, components/mdx/Callout.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md (Pattern 3: plugin chain ordering; Don't Hand-Roll: copy-button transformer; Pitfall 2)
    - .planning/phases/01-foundation-first-pattern-post/01-PATTERNS.md (Shared Patterns: plugin chain ordering)
    - velite.config.ts (current state from Plan 01 — minimal MDX compile)
    - lib/mdx-components.tsx (current typography-only map from Plan 01)
  </read_first>
  <action>
    Extend the MDX compile options in `velite.config.ts` to run the plugin chain in this exact order (Pattern 3 / Pitfall 2): `remarkPlugins: [remarkGfm]`; `rehypePlugins: [rehypeSlug, rehypeAutolinkHeadings, [rehypePrettyCode, { ...options }]]`. `rehype-slug` MUST come before `rehype-autolink-headings` (autolink depends on the ids slug adds). Configure `rehype-pretty-code` with a light/dark theme pair (e.g. a `themes: { light, dark }` object) so highlighting respects the active theme, and attach the `@rehype-pretty/transformers` copy-button transformer so code blocks get a copy button with zero hand-rolled client component (Don't Hand-Roll). Confirm the Shiki theme names used are valid Shiki bundled themes.

    Create `components/mdx/Callout.tsx` as a presentational component (a styled aside/note box) usable in the MDX body. Register it plus any code-block/heading presentation components in `lib/mdx-components.tsx`. Do NOT register any live-demo components (deferred to Phase 2 per D-06).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `velite.config.ts` rehypePlugins array lists `rehypeSlug` BEFORE `rehypeAutolinkHeadings`, and `rehypePrettyCode` is present with a light/dark theme configuration and the copy-button transformer
    - `npm run build` compiles the MDX with the plugin chain and produces highlighted `<pre>`/`<code>` HTML in the generated body (no client-side highlighter shipped)
    - `components/mdx/Callout.tsx` exists and is registered in `lib/mdx-components.tsx`
    - `lib/mdx-components.tsx` registers NO live-demo components (grep for demo-registry keys returns none)
  </acceptance_criteria>
  <done>The MDX compile runs the correctly-ordered plugin chain, code blocks emit Shiki-highlighted static HTML with a copy button, and `Callout` is available in MDX. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the complete 8-section Toast Pattern post with static code snippets</name>
  <files>content/patterns/toast-notification-system.mdx</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md (D-04, D-05, D-06 — Toast topic, 8-section template, code-blocks-only)
    - .planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md (content/patterns pattern-source entry)
    - content/patterns/toast-notification-system.mdx (current minimal body from Plan 01 — replace/extend it)
    - velite.config.ts (the schema the frontmatter must satisfy)
  </read_first>
  <action>
    Expand `content/patterns/toast-notification-system.mdx` into the full Pattern post following the exact 8-section template from D-05 / PATT-01, in this order: (1) the problem it solves, (2) when to use / when not to use, (3) trade-offs, (4) common mistakes, (5) accessibility considerations, (6) performance implications, (7) edge cases, (8) implementation considerations. Cover the real depth that motivated choosing Toast over combobox/modal (D-04): queue management, ARIA live regions (`role="status"`/`aria-live="polite"` vs `assertive`), auto-dismiss timing, stacking/positioning, pause-on-hover. Use static fenced code blocks only (D-06) in TS/TSX/JS/HTML/CSS to exercise the highlighter across languages — no screenshots, no GIF/video, no live demo. Use a GFM table for the trade-offs section (remark-gfm is enabled). Optionally use the `<Callout>` component for notes. Keep the prose genuinely production-grade (the "why" and trade-offs, not a tutorial) per the project's Core Value — the author has final say on wording at review (no checkpoint needed per Claude's Discretion in CONTEXT.md). Keep frontmatter valid against the Velite schema (`category: components`, slug `toast-notification-system`, description under 160 chars).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The MDX body contains all eight template sections in the D-05 order (verifiable as eight headings covering: problem, when to use/not, trade-offs, common mistakes, accessibility, performance, edge cases, implementation)
    - The post contains fenced code blocks in more than one language (at least TS/TSX plus one of JS/HTML/CSS) and contains NO image/video/live-demo embeds (code-blocks-only per D-06)
    - Frontmatter validates against the Velite schema and `npm run build` is green
    - The trade-offs section uses a GFM table
  </acceptance_criteria>
  <done>The Toast Pattern post is complete: all eight template sections, real production-grade prose, multi-language static code blocks rendered with syntax highlighting, no live demo. Build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Emit per-post SEO metadata (Open Graph, JSON-LD Article, title/description) and render reading time</name>
  <files>app/patterns/[slug]/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md (Pattern 2: generateMetadata; Code Examples: JSON-LD Article schema injection; Security Domain: dangerouslySetInnerHTML risk assessment)
    - .planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md (D-07 site name, D-08 placeholder domain single-source, D-09 author byline)
    - .planning/phases/01-foundation-first-pattern-post/01-PATTERNS.md (app/patterns/[slug]/page.tsx pattern-source; Shared Patterns: Site URL config, Author byline, Site name)
    - app/patterns/[slug]/page.tsx (current state from Plan 01 — render-only, structured for metadata to be added)
    - lib/content.ts (getPatternBySlug helper)
  </read_first>
  <action>
    Add an exported `async generateMetadata({ params })` to `app/patterns/[slug]/page.tsx` that reads `getPatternBySlug(params.slug)` and returns: `title` (post title), `description` (post description), and `openGraph` (title, description, `type: "article"`, `publishedTime: post.publishedAt`, and a canonical URL built from `process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.com"` — the single domain source per D-08, never hardcoded in multiple places). Include `metadataBase` or a canonical `alternates.canonical` using the same SITE_URL. Return `{}` for an unknown slug.

    In the page component, inject a JSON-LD Article schema via a `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} />` where `jsonLd` has `@context: "https://schema.org"`, `@type: "Article"`, `headline: post.title`, `description: post.description`, `author: { "@type": "Person", name: "Alejandro Arevalo" }` (D-09 — never the git handle `sykes10`), and `datePublished: post.publishedAt`. Per RESEARCH.md Security Domain, this `dangerouslySetInnerHTML` use injects author-controlled, build-time-known data (not user input), so it is acceptable here without additional sanitization.

    Render the post's `readingTime` string in the post header near the title (SITE-03). Use the site name "Frontend Blueprints" (D-07) in the OG `siteName`. Keep route data access through `lib/content.ts`, not `#site/content` directly.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/patterns/[slug]/page.tsx` exports an async `generateMetadata` returning `title`, `description`, and `openGraph` (with `type: "article"` and `publishedTime`); the canonical/OG URL is built from `process.env.NEXT_PUBLIC_SITE_URL` (grep: the literal `https://example.com` appears only as the env fallback default, not hardcoded across multiple URL constructions)
    - The page injects a JSON-LD `<script type="application/ld+json">` whose author name is exactly `Alejandro Arevalo` (grep: `Alejandro Arevalo` present; `sykes10` absent from the file)
    - The rendered post header displays the `readingTime` string
    - `npm run build` is green and the page still imports from `lib/content.ts`, not `#site/content`
  </acceptance_criteria>
  <done>Each post route emits correct OG tags, a JSON-LD Article schema authored by "Alejandro Arevalo", and title/description, and the reading time is visible in the post header. Build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author MDX/frontmatter -> build pipeline | Frontmatter (title/description/publishedAt) flows into both the rendered page and the JSON-LD / OG metadata; all author-controlled, validated by Velite's Zod schema at build time. |
| build output -> browser/social crawlers | OG tags and the JSON-LD `<script>` are served to social crawlers and search engines; content is author-controlled, build-time-known data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering / Information Disclosure | JSON-LD injection via `dangerouslySetInnerHTML` (Task 3) | low | accept | The interpolated JSON-LD is built from author-controlled frontmatter known at build time, not user-submitted data, so the typical untrusted-HTML XSS risk does not apply (per RESEARCH.md Security Domain). `JSON.stringify` serializes the object. If a future phase interpolates reader-submitted content here, escaping/sanitization must be added at that time. |
| T-01-04 | Tampering | Shiki/rehype-pretty-code MDX compile (Task 1) | low | mitigate | Highlighting happens at build time over author-controlled code blocks and is baked into static HTML; no client-side highlighter or user-supplied grammar is loaded at runtime. |
</threat_model>

<verification>
- `npm run build` is green (plugin chain compiles, post validates, metadata + JSON-LD type-check).
- The Plan 01 Playwright smoke suite still passes (reading-time presence assertion now backed by a real rendered value).
- Manual (justified manual-only per VALIDATION.md): visually confirm accurate syntax highlighting across TS/TSX/JS/HTML/CSS code blocks (FOUND-02), and review the rendered post against the 8-section template checklist (PATT-01).
- Manual/automated SEO spot-check: confirm `generateMetadata` output shape (OG tags, title/description) and that the JSON-LD parses as valid Article schema (SITE-01).
</verification>

<success_criteria>
- Code blocks render with accurate Shiki syntax highlighting for TS/TSX/JS/HTML/CSS (FOUND-02).
- The post follows the full 8-section template with static code snippets only (PATT-01, PATT-03).
- Each post has correct SEO metadata: OG tags, JSON-LD Article (author "Alejandro Arevalo"), title/description (SITE-01).
- The post displays an estimated reading time (SITE-03).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-first-pattern-post/01-02-SUMMARY.md` when done.
</output>
