---
phase: 01-foundation-first-pattern-post
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/rss.xml/route.ts
  - app/sitemap.ts
  - app/robots.ts
  - tests/feed.spec.ts
autonomous: true
requirements:
  - SITE-02

must_haves:
  truths:
    - "GET /rss.xml returns valid RSS XML listing the published Pattern post"
    - "GET /sitemap.xml returns a sitemap listing the post URL"
    - "GET /robots.txt is served and references the sitemap"
    - "Post titles/descriptions containing &, <, or > are XML-escaped in the RSS feed"
  artifacts:
    - app/rss.xml/route.ts
    - app/sitemap.ts
    - app/robots.ts
    - tests/feed.spec.ts
  key_links:
    - "app/rss.xml/route.ts and app/sitemap.ts both read getAllPatterns() (lib/content.ts) -> the same Velite-compiled collection"
    - "all post URLs are built from process.env.NEXT_PUBLIC_SITE_URL (single source per D-08)"
    - "app/robots.ts references the sitemap URL built from the same SITE_URL"
---

## Phase Goal

**As a** visiting engineer or hiring manager, **I want to** read one fully realized, correctly themed, syntax-highlighted, SEO-correct Pattern post, **so that** I can learn a production-grade mental model and judge how the author thinks.

## This Plan's Slice (Feed + crawler discoverability)

Building on the Walking Skeleton from Plan 01, this slice makes the content discoverable by feed readers and search-engine crawlers: a working RSS feed, a sitemap, and a robots file — all reading the same Velite collection and using the single `NEXT_PUBLIC_SITE_URL` source so the placeholder domain swaps trivially later. Runs in parallel with Plan 02 (no shared files).

<objective>
Add an RSS feed Route Handler, a sitemap, and a robots file that read the Velite-compiled Pattern collection and build all URLs from a single configurable site-URL source.

Purpose: Satisfy SITE-02 (RSS + sitemap) and round out crawler discoverability so the one Pattern post is findable by feed readers and search engines.
Output: Working `/rss.xml`, `/sitemap.xml`, and `/robots.txt` endpoints plus an automated feed/sitemap smoke test.
</objective>

<execution_context>
@C:/Users/Alex/workspace/blog/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Alex/workspace/blog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md
@.planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md
@.planning/phases/01-foundation-first-pattern-post/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files created by THIS plan (new — exclude from drift/source-grounding checks):

- New files: `app/rss.xml/route.ts` (RSS Route Handler with `GET`), `app/sitemap.ts` (Next.js sitemap convention file, default export returning `MetadataRoute.Sitemap`), `app/robots.ts` (Next.js robots convention file, default export returning `MetadataRoute.Robots`), `tests/feed.spec.ts` (Playwright smoke test)
- Local helper: `escapeXml(s: string)` in `app/rss.xml/route.ts`
- Env var consumed (already defined in Plan 01): `NEXT_PUBLIC_SITE_URL`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the RSS feed Route Handler with XML escaping</name>
  <files>app/rss.xml/route.ts</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md (Code Examples: RSS feed Route Handler; Don't Hand-Roll: XML escaping; Security Domain: RSS/XML injection mitigation)
    - .planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md (D-07 site name, D-08 single site-URL source)
    - .planning/phases/01-foundation-first-pattern-post/01-PATTERNS.md (app/rss.xml/route.ts pattern-source; Shared Patterns: Site URL config, Site name)
    - lib/content.ts (getAllPatterns helper from Plan 01)
  </read_first>
  <action>
    Create `app/rss.xml/route.ts` exporting an async `GET()` that builds an RSS 2.0 XML document from `getAllPatterns()` (use the `lib/content.ts` helper, not `#site/content` directly). The channel `<title>` is "Frontend Blueprints" (D-07) with a channel description for the site. Each `<item>` has `<title>`, `<link>`, `<description>`, `<pubDate>` (RFC-822 / `toUTCString()` of `publishedAt`), and `<guid>`. Build every URL from `const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.com"` (single source per D-08 — the literal placeholder appears only as the fallback default). Define a local `escapeXml(s)` helper that escapes `&`, `<`, and `>` and apply it to every interpolated title/description (required per Security Domain — author-controlled but must be escaped so a `&` or `<` in a title cannot produce invalid XML). Return a `Response` with header `Content-Type: application/xml; charset=utf-8`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/rss.xml/route.ts` exports an async `GET` returning a `Response` with `Content-Type: application/xml; charset=utf-8`
    - The channel `<title>` is exactly `Frontend Blueprints`
    - A local `escapeXml` helper escaping `&`, `<`, `>` exists and is applied to interpolated title/description fields
    - All URLs derive from `process.env.NEXT_PUBLIC_SITE_URL` (the literal `https://example.com` appears only as the `??` fallback)
    - Data is read via `getAllPatterns()` from `lib/content.ts`, not `#site/content` directly
  </acceptance_criteria>
  <done>`/rss.xml` serves valid, XML-escaped RSS 2.0 listing the Pattern post, with all URLs from the single site-URL source. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the sitemap and robots convention files</name>
  <files>app/sitemap.ts, app/robots.ts</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-RESEARCH.md (Code Examples: sitemap.ts convention file)
    - .planning/phases/01-foundation-first-pattern-post/01-CONTEXT.md (D-08 single site-URL source)
    - .planning/phases/01-foundation-first-pattern-post/01-PATTERNS.md (app/sitemap.ts and app/robots.ts pattern-source entries)
    - lib/content.ts (getAllPatterns helper from Plan 01)
  </read_first>
  <action>
    Create `app/sitemap.ts` as the Next.js sitemap convention file: a default export returning `MetadataRoute.Sitemap`. Map `getAllPatterns()` to `{ url, lastModified }` entries where `url` is `${SITE_URL}/patterns/${p.slug}` and `lastModified` is `p.publishedAt`; also include the home (`/`) and the patterns listing (`/patterns`) URLs. Use `const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.com"` (single source per D-08).

    Create `app/robots.ts` as the Next.js robots convention file: a default export returning `MetadataRoute.Robots` that allows all user-agents and points `sitemap` at `${SITE_URL}/sitemap.xml` using the same SITE_URL source. (robots.ts is not explicitly required by REQUIREMENTS.md but is low-cost and conventionally paired with sitemap.ts per PATTERNS.md.)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/sitemap.ts` default-exports a function returning `MetadataRoute.Sitemap` including the post URL, home, and patterns-listing URLs, all built from `process.env.NEXT_PUBLIC_SITE_URL`
    - `app/robots.ts` default-exports a function returning `MetadataRoute.Robots` whose `sitemap` field points at `${SITE_URL}/sitemap.xml`
    - Both files read patterns via `getAllPatterns()` from `lib/content.ts` (sitemap), not `#site/content` directly
    - `npm run build` is green and produces `/sitemap.xml` and `/robots.txt` routes
  </acceptance_criteria>
  <done>`/sitemap.xml` lists the post and key pages, and `/robots.txt` references the sitemap — both using the single configurable site-URL source. Build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Add an automated feed/sitemap smoke test</name>
  <files>tests/feed.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation-first-pattern-post/01-VALIDATION.md (SITE-02 is the highest-value automated target — RSS/sitemap XML validity)
    - playwright.config.ts (created in Plan 01 — reuse its webServer/baseURL setup)
    - app/rss.xml/route.ts and app/sitemap.ts (the endpoints under test)
  </read_first>
  <action>
    Create `tests/feed.spec.ts` using the Playwright config from Plan 01. Add smoke assertions for SITE-02: (1) GET `/rss.xml` responds 200 with an `application/xml` content type and the body contains the channel title "Frontend Blueprints" and the post link/slug `toast-notification-system`; (2) GET `/sitemap.xml` responds 200 and the body contains the post URL; (3) GET `/robots.txt` responds 200 and references `sitemap`. Keep it fast and dependency-free (plain request assertions, no browser navigation needed).
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/feed.spec.ts` asserts `/rss.xml` returns 200 + XML content type + contains "Frontend Blueprints" and `toast-notification-system`
    - It asserts `/sitemap.xml` returns 200 and contains the post URL
    - It asserts `/robots.txt` returns 200 and references the sitemap
    - `npm test` runs the full Playwright suite (Plan 01 smoke + this feed spec) green
  </acceptance_criteria>
  <done>An automated smoke test confirms the RSS feed, sitemap, and robots endpoints serve valid content listing the post. `npm test` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author frontmatter -> RSS/sitemap output | Post title/description/slug flow into the XML feed and sitemap; author-controlled, validated by Velite at build time. |
| feed/sitemap output -> feed readers & crawlers | XML is served to external feed readers and search-engine crawlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | RSS/XML injection via unescaped title/description (Task 1) | low | mitigate | The `escapeXml()` helper escapes `&`, `<`, `>` in every interpolated field before it enters the XML string (per RESEARCH.md Security Domain) — required so a title containing `&` or `<` cannot produce malformed/invalid XML. Author-controlled input, but escaped defensively. |
</threat_model>

<verification>
- `npm run build` is green (RSS route, sitemap, robots all type-check and generate).
- `npm test` is green (feed/sitemap/robots smoke assertions pass against the real built app).
- Manual spot-check (optional): `curl localhost:3000/rss.xml` and `curl localhost:3000/sitemap.xml` return valid XML with the one post listed (per VALIDATION.md SITE-02).
</verification>

<success_criteria>
- The site exposes a working RSS feed and sitemap listing the Pattern post (SITE-02).
- All feed/sitemap/robots URLs derive from the single `NEXT_PUBLIC_SITE_URL` source (D-08).
- An automated smoke test verifies feed/sitemap/robots serve correct content.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-first-pattern-post/01-03-SUMMARY.md` when done.
</output>
