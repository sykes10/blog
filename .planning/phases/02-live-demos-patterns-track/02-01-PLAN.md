---
phase: 02-live-demos-patterns-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/demos/OptimisticUpdatesDemo.tsx
  - lib/mdx-components.tsx
  - content/patterns/optimistic-updates.mdx
  - tests/patterns-demo.spec.ts
autonomous: true
requirements: [PATT-02]
must_haves:
  truths:
    - A visitor can click the demo's action button and see the UI update immediately (optimistic state) before the simulated network call resolves
    - When the simulated call fails, the UI reverts to its prior state AND a visible, aria-live-announced error message explains the failure
    - The Optimistic Updates Pattern post is live at /patterns/optimistic-updates in the Behaviours category, spanning a second category alongside the existing Components Toast post
  artifacts:
    - components/demos/OptimisticUpdatesDemo.tsx
    - content/patterns/optimistic-updates.mdx
    - tests/patterns-demo.spec.ts
  key_links:
    - "getMDXComponents() in lib/mdx-components.tsx registers OptimisticUpdatesDemo so <OptimisticUpdatesDemo /> is usable inline in MDX (D-04: demo components register via getMDXComponents(); demo wrapper is \"use client\", MDX post body stays a Server Component)"
    - "The MDX post embeds <OptimisticUpdatesDemo /> as a client island inside the Server-Component-rendered post body"
---

<objective>
Deliver the first live, interactive demo in the Pattern registry (PATT-02, D-02, D-05) and ship the second Pattern post — Optimistic Updates in the Behaviours category (D-01, D-03) — that embeds it.

Purpose: Proves the demo-registry architecture end-to-end (a real `"use client"` island hydrating inside an otherwise-static Server-Component MDX body) and brings the Patterns track to two posts spanning two categories, satisfying the "multiple posts, more than one category" success criterion.

Output: A working `OptimisticUpdatesDemo` client component using React 19's `useOptimistic` + `startTransition` with automatic rollback and an aria-live error announcement, registered in `getMDXComponents()`, and a complete `optimistic-updates.mdx` post following the Phase 1 8-section Pattern template.
</objective>

<execution_context>
@C:/Users/Alex/workspace/blog/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Alex/workspace/blog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-demos-patterns-track/02-CONTEXT.md
@.planning/phases/02-live-demos-patterns-track/02-RESEARCH.md
@.planning/phases/02-live-demos-patterns-track/02-PATTERNS.md

# Analog source files the executor replicates patterns from:
@components/theme/ThemeToggle.tsx
@components/mdx/Callout.tsx
@lib/mdx-components.tsx
@content/patterns/toast-notification-system.mdx
@tests/smoke.spec.ts
</context>

## Artifacts this phase produces (Plan 01)

- `components/demos/OptimisticUpdatesDemo.tsx` — exported client component `OptimisticUpdatesDemo`
- `lib/mdx-components.tsx` — new import + registry entry `OptimisticUpdatesDemo` in `getMDXComponents()`
- `content/patterns/optimistic-updates.mdx` — new Pattern post, slug `optimistic-updates`, category `behaviours`
- `tests/patterns-demo.spec.ts` — Playwright spec covering the optimistic demo interaction

<tasks>

<task type="auto">
  <name>Task 1: Write failing end-to-end Playwright spec for the optimistic demo</name>
  <files>tests/patterns-demo.spec.ts</files>
  <read_first>
    - tests/smoke.spec.ts (copy the test.describe / page.goto / getByRole / waitForFunction shape and the "returns 404 for unknown slug" style)
    - .planning/phases/02-live-demos-patterns-track/02-RESEARCH.md (Validation Architecture → PATT-02 rows; the demo exposes a like/toggle button)
    - .planning/phases/02-live-demos-patterns-track/02-PATTERNS.md ("Click + state-change wait pattern", lines 314-326)
  </read_first>
  <action>
    Create tests/patterns-demo.spec.ts as a Playwright spec that navigates to /patterns/optimistic-updates and drives the demo. Include at minimum: (a) a test tagged "optimistic demo" that asserts the page responds 200 and the demo's action button is visible via getByRole("button"); (b) a test that clicks the action button and uses page.waitForFunction to assert the button's visible label or aria-pressed attribute changes immediately after click (the optimistic update), mirroring the theme-toggle waitForFunction pattern in smoke.spec.ts. Reference the button by an accessible name the demo will expose (e.g. a name matching /like|undo|save/i) — pick the exact matcher to align with the button label chosen in Task 2, so Task 2 must satisfy this spec. Use a role="status" locator to assert an aria-live status region exists in the DOM on load (present-but-empty), since the demo must render the live region before content changes (RESEARCH.md Pitfall 4). This spec MUST fail now because the post and demo do not yet exist — that is the RED state. Do not weaken assertions to make it pass prematurely.
  </action>
  <verify>
    <automated>npx playwright test tests/patterns-demo.spec.ts 2>&1 | grep -qiE "failed|error|no tests|not found|timed out" && echo "RED confirmed: spec fails as expected" || echo "UNEXPECTED PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/patterns-demo.spec.ts exists and contains at least one test in a test.describe block referencing /patterns/optimistic-updates
    - The spec locates the demo action button by role and accessible name, and asserts a state change after click via waitForFunction
    - The spec asserts a role="status" aria-live region is present on page load
    - Running the spec now fails (RED) because the target post/demo does not yet exist
  </acceptance_criteria>
  <done>The Playwright spec for the optimistic demo exists, encodes the click→optimistic-update→revert behavior and the aria-live region presence, and fails against the current (demo-less) build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the OptimisticUpdatesDemo client island and register it</name>
  <files>components/demos/OptimisticUpdatesDemo.tsx, lib/mdx-components.tsx</files>
  <read_first>
    - components/theme/ThemeToggle.tsx (client-island conventions: "use client" on line 1, named function export, onClick bound to a named handler, descriptive aria-label reflecting resulting state, Tailwind dark: class pairs, no-layout-shift discipline)
    - lib/mdx-components.tsx (the exact one-line Callout registration in getMDXComponents(); update the comment on line 7 that says demo entries are deferred to Phase 2)
    - .planning/phases/02-live-demos-patterns-track/02-RESEARCH.md (Architecture Patterns Pattern 1 — useOptimistic + startTransition full example; Pitfalls 1 and 4; Security Domain "simulated network failure must reject with a generic authored message, never error.stack")
    - components/mdx/Callout.tsx (JSDoc-per-component doc style and Tailwind CSS-variable token usage)
  </read_first>
  <behavior>
    - Clicking the action button updates the visible UI immediately (optimistic state shown before the simulated call resolves)
    - On simulated success, the committed state matches the optimistic state (no revert)
    - On simulated failure, the UI reverts to the prior committed state automatically (via useOptimistic re-render from unchanged base state, no manual snapshot/restore)
    - On failure, a visible error message renders AND is announced through a role="status" aria-live="polite" region that already existed in the DOM before the failure
    - The button is keyboard-operable (native button element, reachable by Tab, activated by Enter/Space)
  </behavior>
  <action>
    Create components/demos/OptimisticUpdatesDemo.tsx as a "use client" component named OptimisticUpdatesDemo. Model its structure on ThemeToggle.tsx (directive first line, named export, named onClick handler) and its doc/JSDoc + Tailwind CSS-variable styling on Callout.tsx. Use React 19 useState for the committed state, useOptimistic for the optimistic view, and wrap the setter plus the async simulated call in startTransition(async () => { ... }) — calling the optimistic setter outside a transition is Pitfall 1 and is forbidden. Implement a simulated network call as a Promise + setTimeout that randomly rejects (e.g. ~40% of the time) with a deliberately authored generic Error message such as "Update failed — please try again"; NEVER surface error.stack or internal detail (Security Domain, Information Disclosure). Choose a concrete interaction per D-05 (Claude's discretion) — a like-toggle or todo-item-toggle is the natural fit; the action button's accessible name and label MUST match the matcher the Task 1 spec expects. Render a persistent role="status" aria-live="polite" element that is always in the DOM (even when empty) so screen readers announce the rollback message when it appears (RESEARCH.md Pitfall 4 / Anti-Patterns "inject aria-live at moment of change" is forbidden). Show a visible pending indicator while the transition is in flight and a visible error message on failure. Use only Tailwind CSS-variable tokens (text-(--foreground), text-(--muted), border-(--border)) and zinc/accent palette classes with dark: pairs — no hardcoded hex. Then in lib/mdx-components.tsx add import { OptimisticUpdatesDemo } from "@/components/demos/OptimisticUpdatesDemo"; and add OptimisticUpdatesDemo, to the object returned by getMDXComponents(), following the exact one-line Callout registration style; update the deferred-to-Phase-2 comment to reflect that the demo registry is now populated.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -qi "OptimisticUpdatesDemo" && echo "TYPE ERROR in demo" || echo "types ok"</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/demos/OptimisticUpdatesDemo.tsx starts with the "use client" directive on line 1 and exports a named function OptimisticUpdatesDemo
    - The component imports useOptimistic and startTransition from "react" and wraps the optimistic setter + async call in startTransition
    - A role="status" aria-live="polite" element is rendered unconditionally (present in the DOM before any state change)
    - The simulated failure path throws/rejects with an authored generic message; grep of the component source shows no "error.stack" usage: `grep -c 'error.stack' components/demos/OptimisticUpdatesDemo.tsx` returns 0
    - lib/mdx-components.tsx imports OptimisticUpdatesDemo and includes it in the getMDXComponents() return map
    - `npm run build` passes
  </acceptance_criteria>
  <done>OptimisticUpdatesDemo is a working "use client" island using useOptimistic+startTransition with automatic rollback, a persistent aria-live status region, a visible error state, and a generic (non-leaking) failure message; it is registered in getMDXComponents() and the build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Author the Optimistic Updates Pattern post and turn the spec green</name>
  <files>content/patterns/optimistic-updates.mdx, tests/patterns-demo.spec.ts</files>
  <read_first>
    - content/patterns/toast-notification-system.mdx (frontmatter block shape and the 8-section Pattern template heading structure: problem → when to use / when not to use → trade-offs → common mistakes → accessibility → performance → edge cases → implementation considerations; and how <Callout> is embedded inline)
    - velite.config.ts (Pattern schema: title, slug s.slug("patterns"), description max 160 chars, category enum must be "behaviours", tags string array, publishedAt isodate)
    - .planning/phases/02-live-demos-patterns-track/02-RESEARCH.md (Pattern 1 note on the useOptimistic reducer overload — belongs in the post's "implementation considerations" section; Pitfall 4 — the rollback must be legible)
    - components/demos/OptimisticUpdatesDemo.tsx (the exact component/tag name and any required props, to embed it correctly)
  </read_first>
  <action>
    Create content/patterns/optimistic-updates.mdx. Frontmatter must set title "Optimistic Updates" (or similar), slug "optimistic-updates", a description ≤160 chars, category "behaviours" (D-01/D-03 — this is the second category alongside the Toast post's Components), a tags array, and publishedAt as an ISO date. Follow the exact 8-section Pattern template used by toast-notification-system.mdx (the problem it solves → when to use / when not to use → trade-offs → common mistakes → accessibility considerations → performance implications → edge cases → implementation considerations). Embed the live demo by writing <OptimisticUpdatesDemo /> as inline JSX in the body (the same way the Toast post embeds <Callout>), placed near the top of the article so the reader interacts with it early. Content must teach the production mental model of optimistic UI (why it feels instant, the rollback-on-error contract, when NOT to use it), and the "implementation considerations" section should mention React 19's useOptimistic reducer overload for the advanced case (RESEARCH.md Pattern 1 note). Use <Callout> where an aside adds value, matching the Toast post's usage. After the post renders, run the Task 1 spec; if the button accessible-name matcher or aria-live assertions do not match the demo, reconcile by adjusting the spec matcher to the real accessible name (do not weaken the behavioral assertions) — the spec must now pass GREEN.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run build && npx playwright test tests/patterns-demo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - content/patterns/optimistic-updates.mdx exists with frontmatter category "behaviours" and slug "optimistic-updates"
    - The post body contains the <OptimisticUpdatesDemo /> tag and follows the 8-section Pattern template heading structure
    - The description frontmatter field is ≤160 characters (Velite schema max)
    - `npm run build` succeeds (Velite validates the frontmatter and next-mdx-remote compiles the body including the embedded demo)
    - `npx playwright test tests/patterns-demo.spec.ts` passes GREEN (page loads 200, demo button drives an optimistic update, aria-live region present)
  </acceptance_criteria>
  <done>The Optimistic Updates post is live at /patterns/optimistic-updates in the Behaviours category, embeds the working demo, follows the Pattern template, builds cleanly, and the demo Playwright spec passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author MDX → rendered page | Author-controlled MDX content compiled at build time; same trust model as the existing Toast post (no user input crosses here) |
| demo interaction → in-browser simulated network | Entirely client-side; the "network call" is a local Promise+setTimeout, no real backend, no external data crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | OptimisticUpdatesDemo simulated failure path | low | mitigate | Simulated rejection throws a deliberately authored generic message ("Update failed — please try again"); never surface error.stack or internal detail. This is teaching code a reader may copy verbatim, so the safe pattern is demonstrated. Enforced by acceptance grep on error.stack. |
| T-02-02 | Tampering | content/patterns/optimistic-updates.mdx (author-controlled MDX) | low | accept | MDX is author-controlled, build-time-known content — identical trust boundary to the accepted Phase 1 Toast post and JSON-LD injection. No new dynamic/user-supplied content is rendered. |
| T-02-SC | Tampering | npm installs | low | accept | Phase 2 adds zero new npm packages (RESEARCH.md Package Legitimacy Audit: not applicable — all primitives are already-installed React 19 / Next.js 16 built-ins). No install tasks; no [ASSUMED]/[SUS] packages to gate. |
</threat_model>

<verification>
- `npm run build` passes (Velite frontmatter validation + MDX compilation of the embedded client island).
- `npx playwright test tests/patterns-demo.spec.ts` passes: page 200, demo button drives an optimistic UI change, aria-live status region present.
- `/patterns/optimistic-updates` renders in the Behaviours category, giving the track two posts across two categories.
- Manual (phase gate, not this plan's automated verify): keyboard-only pass (button reachable via Tab, activatable via Enter/Space) and NVDA+Firefox pass confirming the rollback/error message is announced through the aria-live region.
</verification>

<success_criteria>
- A visitor can interact with a live, real-implementation optimistic-update demo embedded in a Pattern post (PATT-02, D-02).
- The demo shows optimistic state, simulated network delay, and automatic rollback on error with a visible + announced failure message (D-02).
- The Optimistic Updates post is live in the Behaviours category, so the Patterns track spans two categories (D-01, D-03).
- The demo is registered via getMDXComponents() as the first live demo in the registry (D-04, D-05).
</success_criteria>

<output>
Create `.planning/phases/02-live-demos-patterns-track/02-01-SUMMARY.md` when done
</output>
