---
phase: quick-260701-va7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "A top-level DESIGN.md exists at repo root, sibling to package.json and README"
    - "Every color, font, spacing, and width value in DESIGN.md is traceable to actual code (globals.css, layout.tsx, or page components) — no invented values"
    - "The doc distinguishes what is DECIDED (in code today) from what is PROPOSED (not yet built, e.g. Blueprint post layout, demo islands)"
  artifacts:
    - "DESIGN.md at C:/Users/Alex/workspace/blog/DESIGN.md"
  key_links:
    - "DESIGN.md token values must match app/globals.css @theme + :root/.dark blocks exactly"
    - "DESIGN.md layout widths must match app/layout.tsx (max-w-5xl) and app/patterns/[slug]/page.tsx (max-w-3xl)"
---

<objective>
Create a new top-level `DESIGN.md` at the repo root documenting the visual/UI design system for Frontend Blueprints. This is a living, project-wide reference (like PROJECT.md), not a phase-scoped UI-SPEC — future phases (including in-progress Phase 02 Live Demos & Patterns Track) will reference it for visual consistency.

Purpose: Give a single source of truth for typography, color, spacing, component visual language, and layout conventions, grounded in the actual codebase rather than aspirational invention.

Output: `DESIGN.md` at repo root.
</objective>

<execution_context>
@C:/Users/Alex/workspace/blog/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Alex/workspace/blog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

# Source-of-truth files the DESIGN.md must be grounded in (read these, transcribe real values):
@app/globals.css
@app/layout.tsx
@app/page.tsx
@app/patterns/[slug]/page.tsx
@components/mdx/Callout.tsx
</context>

<grounded_facts>
These are the ACTUAL values in the repo today (verified during planning). Transcribe faithfully; do not round, rename, or embellish. If you find a discrepancy when reading the source files, the source files win.

Stack: Tailwind CSS v4 (CSS-first, `@theme` in `app/globals.css` — there is NO `tailwind.config.js`). `@tailwindcss/typography` plugin provides `.prose`. Fonts loaded via `next/font/google` in `app/layout.tsx`.

Fonts:
- Display + sans: Inter (`--font-display`, `--font-sans` = `"Inter", system-ui, sans-serif`)
- Mono: `--font-mono` = `"JetBrains Mono", "Fira Code", monospace`

Accent (in `@theme`):
- `--color-accent: oklch(0.55 0.22 280)` (deep violet)
- `--color-accent-light: oklch(0.72 0.18 280)` (dark-mode link/accent variant)

Semantic tokens (`:root` light / `.dark`):
- background: `#ffffff` / `#0f1117`
- foreground: `#111827` / `#f1f5f9`
- muted: `#6b7280` / `#94a3b8`
- border: `#e5e7eb` / `#1e2937`
- surface: `#f9fafb` / `#161b27`

Layout widths (from real components):
- Site chrome (header/footer) + home page: `max-w-5xl`, padding `px-4 sm:px-6 lg:px-8`
- Pattern post reading column: `max-w-3xl` (narrower for readability), `py-12`
- Header height: `h-14`, sticky, `border-b`, `bg-(--background)/90 backdrop-blur-sm`

Typographic scale observed in code:
- Home hero h1: `text-4xl sm:text-5xl font-semibold tracking-tight`
- Post title h1: `text-3xl font-semibold tracking-tight`
- Section h2: `text-xl font-semibold tracking-tight`
- Body/lead: `text-lg` (hero lead), `text-base` (post description), `text-sm` (nav, meta, tags), `text-xs` (reading time, category pill)

Code blocks: `rehype-pretty-code` + Shiki, paired themes `github-light` / `github-dark` (in `app/patterns/[slug]/page.tsx`). CSS in globals.css shows/hides the theme matching the active `.dark` class. `<pre>` gets `border-radius: 0.5rem; padding: 1rem 1.25rem; overflow-x: auto`.

Component visual language — "islands" vs prose:
- The `Callout` MDX component uses `not-prose` to escape typography styles, `rounded-r-lg`, a left accent border (`border-l-4`), tinted background, and a semantic color set (note=blue, tip=green, warning=amber, danger=red). This is the established pattern for a visually distinct block inside prose.
- Pills/tags: `rounded-full bg-zinc-100 dark:bg-zinc-800 px-2.5 py-0.5 text-xs`.
- Primary button (home CTA): `rounded-lg bg-accent px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:opacity-90`.

What is NOT yet decided / built (must be labeled PROPOSED, not asserted as fact):
- There is no Blueprint post route or layout yet — only the `patterns` track (`content/patterns/`, `app/patterns/`). Blueprint page structure is a proposal.
- There is no interactive demo "island" component yet (Phase 02 scope). The only in-prose custom island today is `Callout`. Demo-island visual language should be PROPOSED, extrapolated from the Callout `not-prose` + bordered-surface pattern, and explicitly flagged as not-yet-implemented.
- No explicit numeric spacing scale token set exists beyond Tailwind's defaults; document that spacing relies on Tailwind's default 4px-based scale rather than custom tokens.
</grounded_facts>

<tasks>

<task type="auto">
  <name>Task 1: Verify grounded facts against source, then author DESIGN.md</name>
  <files>C:/Users/Alex/workspace/blog/DESIGN.md</files>
  <action>
Read the five source files listed in `<context>` to confirm the values in `<grounded_facts>` are still accurate (they were captured at plan time). If any value differs, use the source file's value.

Then create `DESIGN.md` at the repo root (sibling to package.json / README, NOT inside `.planning/`). Write it as a living project-wide reference in the same spirit as PROJECT.md. Use these sections:

1. Purpose & Status — one paragraph: what this doc is (project-wide visual reference future phases cite), and a "Status" note stating the design system is partially realized: the site chrome, home page, and Pattern post layout exist with real theme tokens, while the Blueprint track and interactive demo islands are not yet built and their entries here are proposals.

2. Design Principles — 3-5 short principles derived from PROJECT.md's ethos (content-first, RSC-static-by-default with small islands of interactivity, production-grade not tutorial, readable by a hiring manager). Keep grounded in the project's stated values.

3. Typography — document the Inter (display/sans) + JetBrains Mono (mono) choices with the exact `--font-*` token strings, the observed type scale (hero/title/section/body/meta sizes with the real Tailwind classes), and the prose-vs-UI-text distinction (`.prose` from `@tailwindcss/typography` for MDX body via `prose dark:prose-invert max-w-none`; utility-class UI text for chrome/listings).

4. Color Palette — a table of the semantic tokens (background, foreground, muted, border, surface) with BOTH light and dark hex values exactly as in globals.css, plus the accent tokens with their `oklch(...)` values and their roles (accent for primary actions/links; accent-light for dark-mode links). Note the token-consumption syntax actually used in code: `bg-(--background)`, `text-(--foreground)`, `text-accent` / `bg-accent`.

5. Spacing & Layout Scale — state that spacing uses Tailwind v4's default 4px-based scale (no custom spacing tokens defined), and document the real container conventions: `max-w-5xl` for chrome/home/listings, `max-w-3xl` for the Pattern reading column, standard responsive padding `px-4 sm:px-6 lg:px-8`, sticky `h-14` header with `backdrop-blur-sm`.

6. Component Visual Language — describe how distinct "islands" look versus flowing prose, using the real `Callout` component as the canonical example (the `not-prose` escape hatch, left accent border, tinted surface, semantic color families note/tip/warning/danger). Document the pill/tag and primary-button treatments. Then add a clearly-labeled "PROPOSED — not yet built" subsection for interactive demo islands (Phase 02): extrapolate a visual language from the Callout pattern (bordered surface distinct from prose, uses `--surface`/`--border` tokens, `not-prose`, clear demo-vs-code separation) and mark it explicitly as a proposal to be validated when the demo components are built.

7. Layout Conventions by Content Type — document the Pattern post page structure as it exists (category pill + reading time, `text-3xl` title, description, tag row, `prose dark:prose-invert` article body, `max-w-3xl` column, back-link footer). Add a clearly-labeled "PROPOSED" Blueprint post layout (since no Blueprint route exists yet) that reuses Pattern conventions and notes the Blueprint-composes-Patterns cross-linking model from PROJECT.md.

8. Code Block Styling — document the `rehype-pretty-code` + Shiki pipeline, the paired `github-light` / `github-dark` themes, the dark-class-driven show/hide behavior, and the `<pre>` treatment (`border-radius: 0.5rem`, padding, `overflow-x: auto`).

Formatting rules for the doc itself:
- Use tables for token/value mappings (color palette, type scale).
- Every asserted current value must be traceable to a source file; where you state a value, it must match the code.
- Anything not currently in the codebase MUST appear under a heading or inline marker containing the word "PROPOSED" (or "Status: proposed"). Do NOT present proposals as existing fact.
- Do NOT invent numeric values (specific px, new hex colors, new fonts) that are not in the source files or Tailwind's documented defaults.
  </action>
  <verify>
    <automated>test -f C:/Users/Alex/workspace/blog/DESIGN.md &amp;&amp; grep -q "oklch(0.55 0.22 280)" C:/Users/Alex/workspace/blog/DESIGN.md &amp;&amp; grep -q "max-w-3xl" C:/Users/Alex/workspace/blog/DESIGN.md &amp;&amp; grep -qi "PROPOSED" C:/Users/Alex/workspace/blog/DESIGN.md &amp;&amp; grep -q "JetBrains Mono" C:/Users/Alex/workspace/blog/DESIGN.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>
`DESIGN.md` exists at repo root with all 8 sections. The accent oklch value, both max-widths, the mono font, and the semantic token hex values all match the source files exactly. Blueprint layout and demo-island visual language are present and explicitly marked PROPOSED. No fabricated color/font/spacing values appear.
  </done>
</task>

</tasks>

<verification>
- `DESIGN.md` is at the repo root (`C:/Users/Alex/workspace/blog/DESIGN.md`), not under `.planning/`.
- Spot-check three token values against `app/globals.css`: accent oklch, `--foreground` light (`#111827`) and dark (`#f1f5f9`).
- Confirm `max-w-5xl` (chrome/home) and `max-w-3xl` (pattern column) both appear and are attributed to the correct context.
- Confirm every not-yet-built item (Blueprint layout, demo islands) is under a PROPOSED marker.
</verification>

<success_criteria>
A future contributor (or agent planning Phase 02) can open `DESIGN.md` and know the exact fonts, color tokens, layout widths, and code-block styling in use, and can clearly tell which parts are decided-in-code versus proposed-for-later — with no invented values contradicting the codebase.
</success_criteria>

<output>
Create `.planning/quick/260701-va7-create-a-new-top-level-design-md-documen/260701-va7-SUMMARY.md` when done.
</output>
