---
phase: quick-260701-w43
plan: 01
subsystem: content
tags: [copy, style-guide, homepage]
dependency-graph:
  requires: []
  provides: []
  affects: [app/page.tsx]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - app/page.tsx
decisions:
  - "Replaced the em dash in the hero tagline with a colon (WRITING-STYLE.md Section 3 replacement move: colon to introduce an elaboration)"
metrics:
  duration: "< 5 minutes"
  completed: 2026-07-01
status: complete
---

# Quick Task 260701-w43: Fix homepage hero copy em dash Summary

Replaced the em dash in the homepage hero tagline (`app/page.tsx`) with a colon, per `WRITING-STYLE.md` Section 3's ban on em dashes in prose.

## What Changed

The hero tagline in the `<p>` element under the "Hero" section of `app/page.tsx` read:

> "Production-grade mental models for frontend engineers — covering the why and the trade-offs as much as the how."

This was rewritten to:

> "Production-grade mental models for frontend engineers: covering the why and the trade-offs as much as the how."

The colon was chosen as the replacement move from `WRITING-STYLE.md` Section 3 ("To introduce a list or an elaboration, use a colon"), since the clause after the dash elaborates on what the mental models cover. Meaning and tone are preserved exactly; only the punctuation mark changed.

Nothing else in the file was touched, including the Tailwind CSS custom property syntax (`text-(--foreground)`, `text-(--muted)`, `text-(--border)`) used throughout the component.

## Deviations from Plan

None - plan executed exactly as written.

## Verification

Ran the plan's automated verification command:

```bash
! grep -n '—' app/page.tsx && grep -q 'text-(--foreground)' app/page.tsx
```

Result: PASSED — no em dash character remains in the file, and the Tailwind CSS variable syntax is intact.

## Self-Check: PASSED

- FOUND: app/page.tsx (modified, exists)
- FOUND: commit ab5727e (verified via `git log --oneline`)

## Commits

- `ab5727e`: fix(260701-w43): remove em dash from homepage hero copy
