---
phase: quick-260701-w43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The homepage hero tagline in app/page.tsx contains no em dash character"
    - "The Tailwind CSS variable syntax text-(--foreground), text-(--muted), text-(--border) elsewhere in the file is untouched"
  artifacts:
    - "app/page.tsx"
  key_links:
    - "WRITING-STYLE.md Section 3 bans the em dash in prose"
---

<objective>
Fix the homepage hero tagline in `app/page.tsx`, which currently reads:

"Production-grade mental models for frontend engineers — covering the why and the trade-offs as much as the how."

This contains an em dash, which `WRITING-STYLE.md` Section 3 bans. Rewrite the sentence to remove the em dash using one of the guide's replacement moves, keeping the same meaning and a human tone.

Output: The same JSX, one sentence rewritten, everything else byte-identical.
</objective>

<context>
@WRITING-STYLE.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove em dash from homepage hero copy</name>
  <files>app/page.tsx</files>
  <action>
Open `app/page.tsx`. Find the `<p>` tag around line 13-16 containing the hero tagline: "Production-grade mental models for frontend engineers — covering the why and the trade-offs as much as the how."

Rewrite it to remove the em dash, using a WRITING-STYLE.md Section 3 replacement move (period + new sentence, comma, colon before the elaboration, or "and"/"but"). Keep the same meaning: the site gives production-grade mental models that cover the why and trade-offs as much as the how. Keep it short, human, and matching the tone of WRITING-STYLE.md Section 2.

Do not touch anything else in the file, including the Tailwind CSS custom property syntax `text-(--foreground)`, `text-(--muted)`, `text-(--border)` used throughout (these are CSS variable references, not prose, and are unrelated to the punctuation ban).
  </action>
  <verify>
    <automated>! grep -n '—' app/page.tsx && grep -q 'text-(--foreground)' app/page.tsx</automated>
  </verify>
  <done>
`app/page.tsx` contains no em dash character. The hero tagline reads as a natural, human sentence conveying the same meaning. The Tailwind `text-(--foreground)`, `text-(--muted)`, `text-(--border)` CSS variable references are unchanged everywhere they appear. No other line in the file is modified.
  </done>
</task>

</tasks>

<verification>
- Run the verify command: no em dash in the file, CSS variable syntax still present.
- Spot-read the hero paragraph to confirm meaning and tone are preserved.
</verification>

<success_criteria>
Homepage hero copy is em-dash-free and reads naturally. Nothing else in the file changed.
</success_criteria>

<output>
Create `.planning/quick/260701-w43-fix-homepage-hero-copy-in-app-page-tsx-t/260701-w43-SUMMARY.md` when done.
</output>
