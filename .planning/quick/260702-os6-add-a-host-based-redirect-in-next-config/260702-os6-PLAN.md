---
phase: quick-260702-os6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
requirements:
  - QUICK-OS6
must_haves:
  truths:
    - "A request to https://www.frontendspec.dev/any/path?q=1 responds with a 308 permanent redirect to https://frontendspec.dev/any/path?q=1"
    - "Requests to the apex host frontendspec.dev are NOT redirected (no redirect loop)"
    - "The existing Velite build IIFE and reactCompiler/turbopack config remain unchanged and functional"
  artifacts:
    - "next.config.ts exports a nextConfig object containing an async redirects() function"
  key_links:
    - "redirects() host-based `has` matcher on the `host` header value www.frontendspec.dev -> destination https://frontendspec.dev/:path* with permanent: true"
---

<objective>
Add a host-based permanent redirect in next.config.ts so that requests to www.frontendspec.dev are 308-redirected to the apex domain frontendspec.dev, preserving the full path and query string.

Purpose: Replace reliance on the Vercel dashboard's domain-redirect toggle (which the user could not locate in their account UI) with an in-code, version-controlled redirect that ships with the app.

Output: Modified next.config.ts with a new async redirects() function added to the exported nextConfig object.
</objective>

<execution_context>
@C:/Users/Alex/workspace/blog/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Alex/workspace/blog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add host-based www-to-apex redirect to nextConfig</name>
  <files>next.config.ts</files>
  <action>
Add an async `redirects()` method to the exported `nextConfig` object (the one already declaring `reactCompiler` and `turbopack`). Do NOT modify the top-of-file Velite build IIFE, the `isDev`/`isBuild` argv detection, the `reactCompiler: true` key, or the `turbopack.root` key.

The redirects() function must return an array with a single redirect object:
- `source`: `/:path*` so the full path segment is captured and forwarded.
- `has`: an array with one matcher of type `host` whose `value` equals `www.frontendspec.dev`. This restricts the rule to requests whose Host header is the www subdomain, so apex-host requests are never matched (prevents a redirect loop).
- `destination`: `https://frontendspec.dev/:path*` so the captured path is preserved on the absolute apex URL. Next.js forwards the query string automatically for wildcard path redirects, satisfying the query-preservation requirement.
- `permanent`: `true` to emit a 308 permanent redirect.

Keep the return type compatible with NextConfig's redirects signature (an array of redirect objects). Preserve the existing `export default nextConfig;` at the end of the file.
  </action>
  <verify>
    <automated>node -e "const c=require('ts-node/register'); " 2>/dev/null || npx tsc --noEmit -p . 2>&1 | grep -i 'next.config' | grep -c error | grep -qx 0</automated>
  </verify>
  <done>next.config.ts type-checks with no errors on next.config.ts; the exported nextConfig contains an async redirects() returning a single rule with source `/:path*`, a `host` `has` matcher for `www.frontendspec.dev`, destination `https://frontendspec.dev/:path*`, and `permanent: true`; the Velite IIFE, reactCompiler, and turbopack keys are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Added an async redirects() function to next.config.ts that permanently (308) redirects www.frontendspec.dev to the apex frontendspec.dev, preserving path and query, via a host-based `has` matcher.</what-built>
  <how-to-verify>
1. Run the production build and start locally: `npm run build && npm run start` (or `npm run dev`).
2. Simulate the www host against a path with a query string:
   `curl -sI -H "Host: www.frontendspec.dev" "http://localhost:3000/patterns/toast?ref=x"`
   Expected: status `308 Permanent Redirect` and a `location: https://frontendspec.dev/patterns/toast?ref=x` header.
3. Confirm no loop on the apex host:
   `curl -sI -H "Host: frontendspec.dev" "http://localhost:3000/patterns/toast"`
   Expected: NO redirect to itself (normal 200 or app response, not a 308 back to frontendspec.dev).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Type check passes for next.config.ts (`npx tsc --noEmit`).
- Local curl with `Host: www.frontendspec.dev` returns 308 + correct `location` header preserving path and query.
- Local curl with `Host: frontendspec.dev` does not self-redirect (no loop).
- `git diff next.config.ts` shows only the added redirects() function; Velite IIFE and existing keys unchanged.
</verification>

<success_criteria>
www.frontendspec.dev requests permanently (308) redirect to frontendspec.dev preserving path and query, apex requests are unaffected, and the existing Velite/reactCompiler/turbopack configuration is intact.
</success_criteria>

<output>
Create `.planning/quick/260702-os6-add-a-host-based-redirect-in-next-config/260702-os6-SUMMARY.md` when done
</output>
