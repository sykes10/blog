---
phase: quick-260701-w7q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "app/layout.tsx contains no em dash character"
    - "The Tailwind CSS variable syntax (bg-(--background), text-(--foreground), border-(--border), etc.) is untouched"
  artifacts:
    - "app/layout.tsx"
  key_links:
    - "WRITING-STYLE.md Section 3 bans the em dash in prose"
---

<objective>
Fix the two remaining em dashes in `app/layout.tsx` missed during the earlier homepage style pass:

1. Line 25, the page metadata `description`: "Production-grade mental models for frontend engineers — UX, architecture, state management, accessibility, and engineering trade-offs."
2. Line 72, the site footer text: "Frontend Blueprints — production-grade mental models for frontend engineers"

Both violate `WRITING-STYLE.md` Section 3 (no em dash in prose). Rewrite both to remove the em dash, keeping the same meaning and a human tone.

Output: The same file, two strings rewritten, everything else byte-identical.
</objective>

<context>
@WRITING-STYLE.md
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove em dashes from layout.tsx metadata description and footer text</name>
  <files>app/layout.tsx</files>
  <action>
Open `app/layout.tsx`.

1. Find the `metadata.description` string (around line 25): "Production-grade mental models for frontend engineers — UX, architecture, state management, accessibility, and engineering trade-offs." Rewrite it to remove the em dash using a WRITING-STYLE.md Section 3 replacement move (a colon before the list of topics fits naturally here, since the em dash is introducing an elaboration). Keep the same meaning and the list of topics (UX, architecture, state management, accessibility, engineering trade-offs).

2. Find the footer `<p>` text (around line 72): "Frontend Blueprints — production-grade mental models for frontend engineers". Rewrite it to remove the em dash. A colon or a short restructure both work here, match whichever reads most natural given the shorter, tagline-like context (this is footer text, not a full sentence).

Do not touch anything else in the file, including the Tailwind CSS custom property syntax (`bg-(--background)`, `text-(--foreground)`, `border-(--border)`, `text-(--muted)`, the `--font-sans`/`--font-mono` variable names) used throughout. These are CSS variable references and font config, not prose, and are unrelated to the punctuation ban.
  </action>
  <verify>
    <automated>! grep -n '—' app/layout.tsx && grep -q 'text-(--foreground)' app/layout.tsx && grep -q -- '--font-sans' app/layout.tsx</automated>
  </verify>
  <done>
`app/layout.tsx` contains no em dash character. The metadata description and footer text both read as natural, human sentences conveying the same meaning as before. The Tailwind CSS variable syntax and font variable names are unchanged everywhere they appear. No other line in the file is modified.
  </done>
</task>

</tasks>

<verification>
- Run the verify command: no em dash in the file, CSS variable syntax and font config still present.
- Spot-read both rewritten strings to confirm meaning and tone are preserved.
</verification>

<success_criteria>
Both remaining em dashes in app/layout.tsx are removed. Nothing else in the file changed.
</success_criteria>

<output>
Create `.planning/quick/260701-w7q-fix-two-remaining-em-dashes-in-app-layou/260701-w7q-SUMMARY.md` when done.
</output>
